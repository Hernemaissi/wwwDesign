/* mark selected category with check */ 

$("#categories").children().each(function(){
	$(this).removeClass("selected");
});
//console.log("mark", $("#categories #<%= escape_javascript(@selected_category.id.to_s()) %>"));
$("#categories #<%= escape_javascript(@selected_category.id.to_s()) %>").addClass("selected");

/* get parts if any */
$("#parts").html("<%= escape_javascript(render :partial => 'search/filter_category', :locals => {:parts => @parts}) %>");


/* get new ads filtered by selected category*/
filterResults();

//$("#results").empty();
//$("#results").html("<%= escape_javascript(render :partial => 'categories/categoryads', :locals => {:c => @selected_category}) %>");
