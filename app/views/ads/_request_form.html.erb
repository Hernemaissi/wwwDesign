<% unless current_user?(@ad.user) %>
  <div id="follow_form">
  <% if current_user.requested?(@ad.id) %>
    <%= render 'unrequest' %>
  <% else %>
    <%= render 'request' %>
  <% end %>
  </div>
<% end %>